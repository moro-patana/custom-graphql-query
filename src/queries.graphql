query GetStudents {
    students {
     id
     firstName
     lastName
     role
     imageUrl
     linkedinUrl
     twitterUrl
    }
}

mutation DeleteStudent($id: Int!) {
    deleteStudent(id: $id) {
      id
    }
  }